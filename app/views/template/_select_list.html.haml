= select("day", col, template_select(dtemp), {},                     |
  {:onchange => remote_function(                                     |
  :url => url_for(                                                   |
  :action => :change_day, :id => col, :dtemp => dtemp))})            |